---
type: session
domains:
  - persistence-data
  - infrastructure-operations
status: learning
level: 1
confidence: medium
prerequisites:
  - DB 서버와 애플리케이션 서버
related_projects: []
related_sources:
  - Real MySQL 8.0 1
last_reviewed: 2026-08-13
next_review: 2026-08-14
created: 2026-08-13
updated: 2026-08-13
---
# 2026-08-13 20:23 Real MySQL 02-2.4.2 학습

## 시작 위치와 목표

[[Real MySQL 8.0 1]]의 02장 2.4.2를 읽고 system variable의 다섯 속성을 서버 시작, connection 생성, JDBC pool 재사용과 연결한다.

## 다룬 질문

- [[MySQL GLOBAL 변수 변경은 기존 세션에 적용되는가]]

## 사용자 메모 원문

> MySQL서버는 기동하면서 설정 파일을 읽어 메모리나 작동 방식을 초기화하고, 접속된 사용자를 제어하기 위한 값을 별도로 저장함. MySQL 서버에 저장된 이런 값들은 시스템 변수라고하고, SHOW VARIABLES, SHOW GLOBAL VALIABLES로 알 수 있음
>
> 각 변수가 글로별 변수인지, 세션 변수인지 알아야함.
>
> 시스템 변수가 가지는 5가지 속성이 있음
>
> Cmd-Line: MySQL서버의 명령행 인자로 설정될 수 있는지 여부
>
> Option file: MySQL 설정파일(my.cnf(리눅스)/my.ini(윈도우)) 로 제어할 수 있는지 여부 (옵션, 설정, 컨피규레이션 파일 모두 같은말)
>
> System Var: 시스템 변수인지 여부, MySQL 서버의 설정 파일을 작성할 때 각 변수명에 사용된 -(하이픈)이나 언더스코어(_)의 구분에 주의. MySQL 8.0에서는 _를 구분자로 사용함. 명령행 옵션으로 만 사용가능한 설정은 - 구분자로 사용
>
> Var Scope: 시스템 변수의 적용 범위를 나타낸다. 글로벌 / 커넥션(Session) 또는 둘다
>
> Dynamic: 시스템 변수가 동적인지 정적인지 구분.

## 생성하거나 수정한 노트

- [[02-2.4.2 MySQL 시스템 변수의 특징]]
- [[MySQL 시스템 변수의 scope와 생명주기]]
- [[MySQL GLOBAL 변수 변경은 기존 세션에 적용되는가]]
- [[Real MySQL 8.0 1]]
- [[현재 학습 위치]]

## 진단과 교정

- system variable을 서버 작동 방식의 설정값으로 정의하고 다섯 속성을 구분한 부분은 맞다.
- SESSION 변수는 사용자 계정에 저장되는 값이 아니라 connection별 값이다.
- `SHOW VARIABLES`의 기본 scope는 SESSION이며 `SHOW GLOBAL VARIABLES`와 구분해야 한다.
- runtime SQL의 system variable은 `_`만 쓰지만 startup command와 option file에서는 `-`와 `_`가 대부분 호환된다. command-only 여부를 구분하는 문법 규칙은 아니다.
- Dynamic과 재시작 후 persistence는 별도 속성이다.

## level 변경과 근거

level 0 → 1. “MySQL 서버에 저장된 이런 값들은 시스템 변수”라는 문장으로 자신의 정의를 제시했다. GLOBAL에서 새 SESSION으로 값이 초기화되는 과정은 아직 설명하지 않았다.

## 미해결 질문

- GLOBAL 변경이 기존 session에 소급되지 않는 이유는 무엇인가?
- JDBC connection pool에서는 새 GLOBAL 설정이 언제 반영되는가?
- `SET GLOBAL`, `SET PERSIST`, option file의 현재값·재시작 후 값은 어떻게 다른가?

## 다음 세션 첫 질문

`SET GLOBAL autocommit = OFF`를 실행해도 이미 연결된 세션의 `autocommit` 값이 바뀌지 않는 이유는 무엇인가요?
