---
type: session
domains:
  - persistence-data
  - infrastructure-operations
  - performance-resources
status: learning
level: 2
confidence: high
prerequisites:
  - DB 서버와 애플리케이션 서버
related_projects: []
related_sources:
  - Real MySQL 8.0 1
last_reviewed: 2026-08-14
next_review: 2026-08-15
created: 2026-08-14
updated: 2026-08-14
---
# 2026-08-14 10:01 Real MySQL 04 아키텍처

## 사용자 요청

- 04 아키텍처의 모든 하위 절을 한 canonical note에 누적한다.
- 사용자가 보낸 표현은 삭제·교정하지 않고 그대로 보존한다.
- 사진을 적극 배치하되 정리와 학습을 우선한다.
- 추가 지식·암기법·연결 흐름을 분리해 보완한다.
- 확인 질문은 답을 포함한 접힘 toggle로 기록한다.
- 다음부터는 정리 내용만 보내도 이 형식을 자동 적용하도록 skill을 수정한다.

전체 학습 원문은 중복 복사하지 않고 [[4.1 MySQL 엔진 아키텍쳐#사용자 정리 원문]]에 exact text로 보존했다.

## 생성하거나 수정한 노트

- [[4.1 MySQL 엔진 아키텍쳐]]
- [[Real MySQL 8.0 1]]
- [[현재 학습 위치]]
- 이전 session의 시작점 link

## 사용한 사진

- `IMG_7564.JPG`: MySQL server 전체 구조
- `IMG_7565.JPG`: MySQL threading model

두 사진은 Vault local asset에 원본 그대로 복사했다. GitHub 저장소가 public이므로 책 page 원본은 `.gitignore`로 제외하고, note에는 동기화 가능한 Mermaid 설명을 함께 남겼다.

## 추가로 확인한 사실

- Handler status는 handler 요청 횟수이며 physical disk I/O byte가 아니다.
- current `realmysql` 8.0.44는 BACKGROUND 35, FOREGROUND 4였고, foreground 중 `one_connection`은 2개였다.
- `thread_cache_size`는 종료된 connection thread 재사용 cache이며 실행 동시성을 제어하는 thread pool이 아니다.
- default `thread_handling`은 `one-thread-per-connection`; 공식 Enterprise Thread Pool은 별도 상용 plugin이다.
- InnoDB는 buffer pool miss read I/O와 dirty page background flush를 구분한다.

## level 변경과 근거

아키텍처 level 0 → 2.

1. “MySQL 엔진: 머리 역할, 스토리지 엔진: 손발 역할”로 두 계층의 책임을 자기 말로 구분했다.
2. foreground thread가 cache·buffer를 조회하고, InnoDB에서 buffer와 background disk write가 분리되는 요청 흐름을 설명했다.

실패 조건·사용자 증상·지표를 직접 적용한 설명은 아직 없으므로 level 3으로 올리지 않는다.

## 다음 학습 시작점

다음 `4.1.x` 하위 절을 [[4.1 MySQL 엔진 아키텍쳐]]에 append한다. 현재 다음 시작점은 4.1.10이며, `4.2`부터는 `04-아키텍쳐/` 안에 새 canonical note를 만든다.

## 후속 구조 결정

같은 날 독서 노트의 단위를 다시 조정했다.

- 장: `NN-장제목/` 폴더
- 큰 절: `N.N 절제목.md` canonical note
- 하위 절: `N.N.N` 이하를 큰 절 note의 heading으로 누적

따라서 기존의 04장 전체 단일 note는 `04-아키텍쳐/4.1 MySQL 엔진 아키텍쳐.md`로 이동했다. 사용자 원문과 기존 정리 내용은 삭제하거나 다시 작성하지 않고 그대로 옮겼다.

## 추가 캡처 · 4.1.2.2와 4.1.3

- 새 원문은 [[4.1 MySQL 엔진 아키텍쳐#사용자 정리 원문]]에 그대로 append했다.
- `IMG_7566.JPG`를 global memory와 session memory를 비교하는 4.1.3 절에 배치했다.
- OS의 `process 공유 heap + thread별 stack·session allocation` 관계를 Mermaid로 추가했다.
- `DAS`와 `SAN`, buffer의 일반 의미, redo log buffer와 binary log cache 차이에 답했다.
- `insert buffer`의 현재 명칭인 change buffer, 문맥상 `DCL → DML`, foreground logical read와 background read I/O의 차이를 별도 주의로 정리했다.
- 실제 `realmysql 8.0.44`에서 read·write I/O thread가 각각 4이고 buffer pool이 128 MiB인 것을 확인했다.

## 종합 영역 갱신

새 numbered 본문을 암기 카드 위에 삽입하고, 큰 지도·암기 카드·접힌 확인 질문·이어지는 내용·공식 확인 자료를 4.1.3까지의 전체 범위로 갱신했다. 다음 시작점은 4.1.4 plugin storage engine model이다.

level은 2를 유지한다. 내부 역할과 요청·memory 흐름은 설명했지만 장애 상황에서 pending I/O나 thread별 memory 지표를 직접 선택·해석한 사용자 답변은 아직 없다.

## 추가 캡처 · 4.1.4부터 4.1.9

- 새 원문은 [[4.1 MySQL 엔진 아키텍쳐#사용자 정리 원문]]에 표현과 오타를 포함해 그대로 append했다.
- `IMG_7567.jpg`는 query execution pipeline, `IMG_7568.jpg`는 Percona high·low priority queue 절에 배치했다.
- plugin의 종류와 handler boundary, component service registry, parser·preprocessor·optimizer·executor·handler의 책임을 연결했다.
- query cache와 InnoDB buffer pool을 구분하고 MySQL 8.0에서 query cache가 제거된 이유를 기록했다.
- Percona thread group·worker·stall·max·priority queue를 Mermaid로 시각화했다.

## 사용 환경과 공식 자료 확인

현재 `realmysql 8.0.44` container를 읽기 전용으로 확인했다.

```text
MySQL Community Server - GPL
thread_handling = one-thread-per-connection
thread_pool_* variables = 없음
```

따라서 지금까지 Docker 실습이 MySQL Enterprise였던 것은 아니다. AWS 공식 자료도 RDS for MySQL 8.0·8.4가 Community Edition을 지원한다고 밝힌다. Oracle MySQL Enterprise Thread Pool은 상용 plugin이고, Percona Server의 thread pool은 별도 호환 fork 구현이다.

현재 Percona Server 8.0 공식 문서 기준으로 `thread_pool_size`는 CPU core 수, `oversubscribe`는 3, `stall_limit`은 500ms, `max_threads`는 100000이 기본이다. 책과 설치 version 차이를 명시하고 운영에서는 설치 version 문서를 우선하도록 정리했다.

## 가장 중요한 보완 답변

- handler status는 storage engine row-operation 요청이지 SQL 수나 physical I/O가 아니다.
- processor affinity는 OS가 thread의 실행 가능 CPU를 제한하는 기능이며 group 수를 core 수에 맞추는 것만으로 hard pinning되지는 않는다.
- 모든 worker가 막히고 `thread_pool_max_threads`에 도달하면 새 worker를 더 만들지 못한다. statement는 기존 worker가 빌 때까지 queue에 남고 별도 timeout이 먼저 발생할 수 있다.
- active transaction statement를 high-priority로 끝내면 lock 보유 시간이 짧아져 전체 lock contention이 줄 수 있지만 low queue starvation도 관측해야 한다.

## 종합 영역 갱신 · 4.1.9

큰 지도·암기 카드·접힌 확인 질문·이어지는 내용·공식 확인 자료를 4.1.9까지의 전체 범위로 갱신했다. level은 2를 유지한다. 사용자가 architecture boundary와 thread pool 목적을 자기 말로 연결했지만, 실제 운영 지표를 보고 병목을 진단하는 level 3 답변은 아직 확인하지 않았다.

다음 시작점은 4.1.10 transaction-supported metadata다.

## 추가 캡처 · 4.1.10 트랜잭션 지원 메타데이터

- 새 원문은 [[4.1 MySQL 엔진 아키텍쳐#사용자 정리 원문]]에 그대로 append했다.
- MySQL 8.0 data dictionary와 별도 표기가 없는 system table이 InnoDB 기반 `mysql` system schema에 저장되는 구조를 정리했다.
- atomic DDL이 data dictionary update·storage engine operation·binary log write를 하나의 성공 또는 rollback으로 묶는다는 점을 연결했다.
- `Prepare → Perform → Commit → Post-DDL → crash recovery`를 Mermaid로 시각화하고 hidden `mysql.innodb_ddl_log`의 역할을 보완했다.
- atomic DDL과 transactional DDL·online DDL을 구분하고, non-InnoDB DDL과 plugin·component 설치 등 공식 예외를 기록했다.

## 종합 영역 갱신 · 4.1 완료

큰 지도·암기 카드·접힌 확인 질문·이어지는 내용·공식 확인 자료를 4.1.10까지 갱신했다. level은 2를 유지한다. “InnoDB metadata 저장 → transaction·DDL log → crash 후 roll forward 또는 rollback”의 인과는 설명했지만, 실제 DDL 장애·metadata lock 상황에서 지표를 선택한 사용자 답변은 아직 없다.

다음 시작점은 4.2 InnoDB storage engine architecture이며, 같은 `04-아키텍쳐/` 폴더에 새 canonical note를 만든다.

## 추가 캡처 · 4.2.1부터 4.2.3

- 새 canonical note [[4.2 InnoDB 스토리지 엔진 아키텍쳐]]를 만들고 사용자 원문을 표현·오타·URL까지 그대로 보존했다.
- 첨부한 트랜잭션 격리 수준·이상 현상·lock·MVCC 요약 380줄은 local reference로 원문 보존하고, 정리본에는 MySQL 8.0 InnoDB 내용만 다시 조직했다.
- `IMG_7571.JPG`는 InnoDB 전체 구조, `IMG_7572.jpg`~`IMG_7575.jpg`는 UPDATE 전·문장·commit 전 buffer pool과 undo·격리 수준별 SELECT 흐름에 배치했다.
- clustered index leaf의 전체 row, secondary index leaf의 PK와 double lookup을 Mermaid로 시각화했다.
- foreign key 검사가 parent·child index와 lock 범위를 넓혀 deadlock 가능성을 높이는 흐름을 추가했다.
- MVCC를 `read view → current version 또는 undo version`으로 정리하고, buffer pool과 undo log를 양자택일로 이해하지 않도록 바로잡았다.
- 쉬운코드 BJ.44·45·46-1·46-2를 isolation anomaly → lock·2PL → MVCC → `SELECT ... FOR UPDATE` 순서로 MySQL 관점에서 연결했다.
- lost update와 원문의 `write screw`(정확한 용어는 write skew)를 구분하고 atomic DML·optimistic locking·locking read의 선택 기준을 보완했다.

## 종합 영역 갱신 · 4.2.3

4.2 큰 지도·암기 카드·접힌 확인 질문·이어지는 내용·공식 확인 자료를 첫 세 절 기준으로 만들었다. level은 2를 유지한다. 사용자가 MVCC의 비차단 읽기 목적과 locking read 필요성을 연결했지만 실제 `INNODB_TRX`·`History list length`·`data_lock_waits`로 문제를 진단한 답변은 아직 확인하지 않았다.

다음 시작점은 4.2.4 잠금 없는 일관된 읽기(Non-Locking Consistent Read)다.

## 추가 캡처 · 4.2.4와 4.2.5

- 사용자 원문을 오타와 화살표 표현까지 [[4.2 InnoDB 스토리지 엔진 아키텍쳐#사용자 정리 원문]]에 그대로 append했다.
- `IMG_7576.JPG`를 변경 중인 row를 RU는 current version, RC·RR은 undo version으로 읽는 4.2.4 흐름에 배치하고 Mermaid로 재현했다.
- RU는 nonlocking이지만 dirty read가 가능해 엄밀한 consistent read는 아니라는 점을 보완했다.
- plain SELECT의 예외로 locking read, SERIALIZABLE, RR의 `INSERT ... SELECT`·`CREATE TABLE ... SELECT`, DML을 구분했다.
- long transaction이 old read view를 유지해 purge를 지연시키고 `History list length`를 키우는 흐름과 관측 SQL을 추가했다.
- wait-for graph의 cycle, 작은 transaction victim, rollback과 lock 해제를 Mermaid로 시각화했다.
- victim 크기는 공식 문서상 insert·update·delete row 수로 판단하며, 전용 detector thread의 주기적 검사라는 구현 표현은 동작 계약으로 외우지 않도록 보완했다.
- `innodb_table_locks`가 모든 MySQL lock을 감지하는 만능 설정이 아니며 `autocommit=0`·`LOCK TABLES` 문맥에 제한됨을 기록했다.
- detector OFF는 무한 대기가 아니라 `innodb_lock_wait_timeout`까지의 대기로 바뀌고, deadlock은 전체 transaction, timeout은 기본적으로 현재 statement만 rollback한다는 차이를 추가했다.

## 실습 서버 확인 · deadlock 관련 설정

현재 `realmysql 8.0.44` Community Server를 읽기 전용으로 확인했다.

```text
innodb_deadlock_detect      = ON
innodb_lock_wait_timeout    = 50
innodb_table_locks          = ON
innodb_print_all_deadlocks  = OFF
innodb_rollback_on_timeout  = OFF
```

## 종합 영역 갱신 · 4.2.5

큰 지도·암기 카드·접힌 확인 질문·이어지는 내용·공식 자료를 4.2.5까지 갱신했다. level은 2를 유지한다. 사용자가 undo 유지와 detector 비용이라는 failure condition을 설명했지만, 실제 lock wait graph·history·CPU를 보고 설정을 선택한 진단 증거는 아직 없다.

다음 시작점은 4.2.6 자동화된 장애 복구다.

## 추가 캡처 · 4.2.6과 4.2.7.1부터 4.2.7.3

- 사용자 원문은 표현과 오타를 포함해 [[4.2 InnoDB 스토리지 엔진 아키텍쳐#사용자 정리 원문]]에 그대로 append했다.
- `IMG_7577.jpg`·`IMG_7578.JPG`의 forced recovery 1~6 설명은 text로 추출하고 요청대로 별도 asset으로 관리하지 않았다.
- `IMG_7579.JPG`~`IMG_7581.jpg`의 midpoint LRU와 page 탐색·flush list 설명도 text·Mermaid로 재구성한 뒤 별도 asset으로 남기지 않았다.
- `IMG_7582.JPG`의 buffer pool과 redo log 관계 그림만 해당 절의 local book photo로 배치했다. `IMG_7583.jpg`·`IMG_7584.JPG`의 LSN·checkpoint age·capacity 설명은 text와 diagram으로 흡수했다.

## 가장 중요한 보완 · recovery

- 정상 crash recovery의 `redo roll-forward → 미완료 transaction undo rollback`과 손상 시의 forced recovery를 분리했다.
- `innodb_force_recovery`는 repair 기능이 아니라 dump를 위한 emergency startup이다.
- redo 손상이라고 6부터 시작하지 않고, data directory·backup을 보존한 별도 copy에서 1부터 최소 단계로 올린다. 특히 4~6은 data file을 더 손상시킬 수 있는 위험 구간이다.
- 기동에 성공하면 application traffic을 붙이지 않고 data를 dump해 clean instance를 다시 구축한다. backup과 binary log PITR이 우선 복구 수단이다.

## 가장 중요한 보완 · buffer pool과 redo

- buffer pool의 page cache와 dirty page write buffering이라는 두 역할을 분리했다.
- 크기는 memory percentage 하나가 아니라 OS·global memory·connection별 local memory·peak task·OOM 여유의 예산으로 결정한다.
- online resize 단위는 `innodb_buffer_pool_chunk_size × innodb_buffer_pool_instances`이며 downsize는 page 회수를 동반해 특히 민감하다.
- free·LRU·flush list의 서로 다른 책임과 한 dirty page가 LRU·flush list에 동시에 속할 수 있음을 정리했다.
- midpoint insertion의 old 3/8과 `innodb_old_blocks_time`이 scan pollution을 줄이는 과정을 Mermaid로 시각화했다.
- WAL·clean/dirty page·LSN·checkpoint age를 연결하고, 너무 작은 redo capacity가 큰 buffer pool의 write buffering 효과를 제한할 수 있음을 정리했다.
- MySQL 8.0.30 이후 redo 용량은 `innodb_redo_log_capacity`, file은 `#innodb_redo` 구조로 관리됨을 책의 이전 설정과 구분했다.

## 실습 서버 확인 · buffer pool과 redo

현재 `realmysql 8.0.44` Community Server를 읽기 전용으로 확인했다.

```text
innodb_buffer_pool_size       = 128 MiB
innodb_buffer_pool_chunk_size = 128 MiB
innodb_buffer_pool_instances  = 1
innodb_page_size              = 16 KiB
innodb_old_blocks_pct         = 37
innodb_old_blocks_time        = 1000 ms
innodb_redo_log_capacity      = 100 MiB
관측 시점 dirty pages         = 0
관측 시점 checkpoint age      = 0에 가까움
```

## 종합 영역 갱신 · 4.2.7.3

큰 지도·암기 카드·접힌 확인 질문·이어지는 내용·공식 자료를 4.2.7.3까지 갱신했다. level은 2를 유지한다. recovery와 memory·redo 흐름은 인과로 연결했지만, 실제 장애 copy에서 dump·rebuild하거나 workload 지표로 buffer pool·redo 용량을 결정한 답변은 아직 확인하지 않았다.

다음 시작점은 4.2.7.4 버퍼 풀 플러시(Buffer Pool Flush)다.

## 추가 캡처 · 4.2.7.4부터 4.2.8

- 사용자 요청 `이 내용들은 한번 가볍게 읽어볼건데, 나중에 중요한 부분만 읽어보기 좋게 정리해줘. 시스템 변수들은 특히 잘 정리해주고`를 [[4.2 InnoDB 스토리지 엔진 아키텍쳐#사용자 정리 원문]]에 그대로 보존했다.
- `IMG_7585.JPG`부터 `IMG_7593.jpg`까지 9장을 모두 확인했다. text 중심 8장은 핵심을 추출해 재구성하고, `IMG_7592.jpg`의 Doublewrite 작동 그림만 local ignored asset으로 배치했다.
- 4.2.7.4~4.2.8은 정독 노트가 아니라 나중에 빠르게 복습할 수 있도록 30초 요약·인과 diagram·시스템 변수 표 중심으로 구성했다.

## 핵심 정리 · flush와 warmup

- flush list flush는 오래된 dirty page를 기록해 checkpoint를 전진시키고 redo 공간을 재사용한다.
- LRU list flush는 LRU tail을 정리해 새 page를 받을 free frame을 확보한다.
- `innodb_io_capacity`는 device 최대 IOPS가 아니라 평상시 background I/O budget이고, `innodb_io_capacity_max`는 뒤처졌을 때의 burst 상한이다.
- adaptive flushing은 redo 생성률·checkpoint age를 보고 flush 속도를 조절한다. SSD는 보통 `innodb_flush_neighbors=0`을 유지한다.
- buffer pool dump는 page data가 아니라 page ID 목록이며, restart warmup 기능이지 data backup이 아니다.
- `INNODB_CACHED_INDEXES`의 cached page 수는 index 크기나 hit rate와 같지 않다.

## 핵심 정리 · Doublewrite

- redo는 변경 내용을 재실행하지만 torn page의 온전한 base page를 보장하지 않는다. Doublewrite가 완전한 page 사본을 제공하고 그 위에 redo를 적용한다.
- 책의 system tablespace 설명과 달리 MySQL 8.0.20부터는 별도 doublewrite file을 사용한다.
- Doublewrite·redo commit flush·binary log sync·backup은 서로 다른 failure를 다루므로 대체 관계가 아니다.

## 실습 서버 확인 · flush와 Doublewrite

현재 `realmysql 8.0.44` Community Server를 읽기 전용으로 확인했다.

```text
page_cleaners=1                buffer_pool_instances=1
dirty_lwm=10%                  dirty_target=90%
io_capacity=200                io_capacity_max=2000
adaptive_flushing=ON           adaptive_lwm=10%
flush_neighbors=0              lru_scan_depth=1024
dump_at_shutdown=ON            load_at_startup=ON
dump_pct=25                    buffer_pool_filename=ib_buffer_pool
doublewrite=ON                 doublewrite_files=2
innodb_flush_log_at_trx_commit=1
sync_binlog=1
관측 시점 dirty pages·pending write·checkpoint age=0
누적 Doublewrite writes=9,317, pages written=37,164
```

## 공식 자료와 version 차이

- 현재 MySQL 8.0의 buffer pool instance 기본은 환경에 따라 조건부다. non-Windows에서 buffer pool이 1GiB 이상이면 기본 8, 그보다 작으면 1이며 multiple instance는 1GiB 이상에서만 효과가 있다.
- 8.0.20+ Doublewrite는 별도 file, 8.0.30+는 `DETECT_ONLY`와 enabled mode 사이의 online 전환을 지원한다.
- 책의 숫자는 출발점으로만 보존하고 실제 설정은 설치 version의 공식 문서와 workload 지표로 판단한다.

## 종합 영역 갱신 · 4.2.8

큰 지도·암기 카드·접힌 확인 질문·이어지는 내용·공식 자료를 4.2.8까지 갱신했다. level은 2를 유지한다. flush·warmup·Doublewrite의 인과는 연결했지만 실제 traffic에서 dirty ratio·checkpoint age·pending I/O·latency로 설정을 선택한 답변은 아직 확인하지 않았다.

다음 시작점은 4.2.9 언두 로그(Undo Log)다.

## 추가 캡처 · 4.2.9 언두 로그

- 사용자가 작성한 4.2.9·4.2.9.1·4.2.9.2 원문을 [[4.2 InnoDB 스토리지 엔진 아키텍쳐#사용자 정리 원문]]에 그대로 보존했다.
- `IMG_7600.jpg`부터 `IMG_7603.jpg`까지 네 장을 모두 원본 크기로 확인했다. undo tablespace 계층 그림 한 장은 공개 Git에서 제외되는 local asset으로 배치하고, 관리 명령은 Mermaid·표·SQL로 재구성했다. 다음 절인 4.2.10 사진 내용은 이번 정리에 섞지 않았다.
- 책의 MySQL 8.0.14 변경 사항을 현재 MySQL 8.0 공식 문서와 교차 확인했다.

## 핵심 연결 · undo lifecycle

```text
DML → undo 생성 → rollback 또는 MVCC version 재구성
COMMIT → 필요한 update undo는 history에 유지
oldest read view 종료 → purge 가능
purge → 내부 공간 재사용
truncate → 비워진 undo tablespace file의 물리 축소
```

- `COMMIT`과 undo 즉시 삭제를 분리하고, 장기 read-only RR transaction도 purge horizon을 붙잡을 수 있음을 정리했다.
- “100GB DELETE = 정확히 100GB undo”는 1:1 산식이 아니라 대량 DML이 큰 undo·redo·binary log를 만든다는 운영 경고로 보완했다.
- monitoring은 file size 단독이 아니라 oldest transaction age·History list length 추세·open read view·purge·disk·latency를 함께 보는 흐름으로 구성했다.
- undo tablespace → rollback segment → undo slot → undo log 계층과 16KiB page에서 segment당 1024 slots 계산을 시각화했다. 이 값은 실제 동시 transaction 처리량 보장값이 아니라 이론상 allocation 상한임을 명시했다.

## 시스템 변수와 version 차이

- `innodb_undo_tablespaces=2`는 8.0.14부터 deprecated·not configurable이므로 현재 표시값을 튜닝 대상으로 보지 않는다.
- 자동 물리 축소는 `innodb_undo_log_truncate`, `innodb_max_undo_log_size`, `innodb_purge_rseg_truncate_frequency`를 연결해 이해한다.
- `innodb_purge_threads`, `innodb_purge_batch_size`, `innodb_max_purge_lag*`는 원인인 장기 transaction을 해결한 뒤 검토할 고급 운영 변수로 분리했다.
- user-created undo tablespace는 `CREATE → INACTIVE → purge 후 EMPTY → DROP 또는 ACTIVE` 순서로 관리하며 기본 두 tablespace는 drop할 수 없다.

## 실습 서버 확인 · undo

현재 `realmysql 8.0.44` Community Server를 읽기 전용으로 확인했다.

```text
undo tablespaces total/implicit/explicit/active = 2 / 2 / 0 / 2
innodb_undo_001, innodb_undo_002                = 각각 16 MiB
innodb_rollback_segments                       = 128
innodb_undo_log_truncate                       = ON
innodb_max_undo_log_size                       = 1 GiB
innodb_purge_threads                           = 4
관측 시점 active transaction / History list / read views = 0 / 0 / 0
```

idle snapshot이므로 정상 운영 capacity의 증거로 사용하지 않는다.

## 종합 영역 갱신 · 4.2.9

큰 지도·암기 카드·접힌 확인 질문·이어지는 내용·공식 자료를 4.2.9까지 갱신했다. level은 2를 유지한다. undo·read view·purge·truncate의 인과는 연결했지만 실제 장기 transaction 실험이나 production history trend를 근거로 대응을 선택한 사용자 답변은 아직 확인하지 않았다.

다음 시작점은 4.2.10 체인지 버퍼(Change Buffer)다.

## 추가 캡처 · 4.2.10 체인지 버퍼

- 사용자가 작성한 4.2.10 원문을 [[4.2 InnoDB 스토리지 엔진 아키텍쳐#사용자 정리 원문]]에 그대로 보존했다.
- `IMG_7604.jpg`를 원본 크기로 확인하고 `innodb_change_buffering`의 여섯 옵션을 검색 가능한 표로 옮겼다. 관계 그림이 아니라 옵션 목록인 사진이므로 별도 asset은 보관하지 않았다.
- 책 내용을 현재 MySQL 8.0 change buffer 공식 문서와 교차 확인했다.

## 핵심 연결 · secondary index random I/O 지연

```text
DML → clustered index와 secondary indexes 변경
secondary target page가 buffer pool에 있음 → 즉시 변경
page가 없고 buffering 가능 → change entry 기록 후 foreground 반환
page read·background·purge·slow shutdown → 원래 page에 merge
```

- change buffer는 data row나 clustered index 변경을 대신 저장하는 것이 아니라 eligible secondary index page의 변경을 미룬다.
- memory에서는 buffer pool 일부를 사용하고, disk에서는 system tablespace에 영속화되므로 commit·restart와 merge 완료는 별개다.
- UNIQUE index는 성공 반환 전에 중복 검사가 필요해 일반적인 `unique_checks=ON`에서는 대상이 아니다. descending secondary index 예외도 함께 기록했다.
- index write를 없애는 것이 아니라 random I/O를 merge debt로 옮기므로, index 수 자체와 후속 merge 비용을 함께 본다.

## 옵션·실습 서버 확인 · change buffer

현재 `realmysql 8.0.44` Community Server를 읽기 전용으로 확인했다.

```text
innodb_change_buffering       = all
innodb_change_buffer_max_size = 25
unique_checks                 = ON
ibuf_size                     = 1
ibuf_merges                   = 0
merged insert/delete-mark/delete operations = 0/0/0
```

- `all`은 inserts·delete-mark·purge의 physical delete를 모두 buffer한다. `changes`는 inserts+delete-mark이고, `none`은 새 buffering만 중지한다.
- `innodb_change_buffer_max_size=25`는 buffer pool 비율 상한이지 고정 할당량이 아니다.
- 누적 merge 0은 지금까지 해당 조건이 관측되지 않았다는 idle snapshot일 뿐 change buffer의 필요성 판단 근거가 아니다.

## 종합 영역 갱신 · 4.2.10

큰 지도·암기 카드·접힌 확인 질문·이어지는 내용·공식 자료를 4.2.10까지 갱신했다. level은 2를 유지한다. change buffer의 조건·UNIQUE 예외·merge debt 인과는 연결했지만 실제 workload에서 DML latency·buffer pool 압력·merge 추세를 비교해 설정을 선택한 답변은 아직 확인하지 않았다.

다음 시작점은 4.2.11 리두 로그(Redo Log)다.

## 추가 캡처 · 4.2.11 리두 로그 및 로그 버퍼

- 사용자가 직접 작성한 문장을 [[4.2 InnoDB 스토리지 엔진 아키텍쳐#사용자 정리 원문]]에 그대로 보존했다.
- 사진 12장을 전부 확인하고, 밑줄·별표·괄호·여백 질문을 `[사진 표시 전사]`로 사용자 원문과 같은 우선순위로 보존했다.
- 4.2.11에 직접 필요한 표시 사진 5장은 Git에서 제외되는 local asset으로 배치했다. `IMG_7612`이후의 4.2.12 어댑티브 해시 인덱스는 절 경계를 지켜 이번 본문에 섞지 않았다.
- MySQL 8.0 공식 문서로 redo capacity·log buffer·commit flush·archive·disable 제약을 교차 확인했다.

## 핵심 연결 · WAL과 crash recovery

```text
DML → buffer pool dirty page + log buffer redo record
COMMIT → flush policy에 따라 redo write/fsync → ACK
data page → page cleaner·checkpoint가 나중에 batch flush
crash → checkpoint 이후 redo roll-forward → 미완료 transaction undo rollback
```

- `COMMIT` 성공과 data page flush를 분리했다. 기본 설정 1에서 commit에 필요한 것은 redo durability이며 dirty page는 뒤에 flush될 수 있다.
- redo는 committed transaction만의 기록이 아니므로 crash recovery에서 redo·undo가 함께 필요하다.
- redo·undo·binary log를 각각 crash roll-forward·rollback/MVCC·replication/PITR로 구분했다.
- redo archive는 physical backup이 필요한 redo를 circular overwrite에서 보존하는 기능이며 binary log를 대체하지 않는다.

## 시스템 변수·version 차이·실습 server

```text
realmysql version                       = 8.0.44
innodb_flush_log_at_trx_commit          = 1
innodb_flush_log_at_timeout             = 1
innodb_log_buffer_size                  = 16 MiB
innodb_redo_log_capacity                = 100 MiB
Innodb_redo_log_capacity_resized        = 100 MiB
Innodb_redo_log_enabled                 = ON
sync_binlog                             = 1
innodb_redo_log_archive_dirs            = empty
```

- `0·1·2`를 write와 disk flush로 분리했고, `0`·`2`의 N초 유실 window와 interval이 정확한 timer는 아님을 명시했다.
- 8.0.30+는 `innodb_redo_log_capacity`가 구식 `innodb_log_file_size × innodb_log_files_in_group`을 대체하며 `#innodb_redo`의 약 32개 file을 사용한다.
- redo disable은 새 instance의 bulk load 전용이며 운영에서 사용하지 않는다. crash 시 data loss·corruption·시작 거부 위험이 있다.

## 종합 영역 갱신 · 4.2.11

한 문장·큰 지도·암기 카드·접힌 확인 질문·이어지는 내용·공식 자료를 4.2.11까지 갱신했다. level은 2를 유지한다. redo·log buffer·dirty page·checkpoint·crash recovery의 인과는 연결했지만, 실제 workload의 commit latency·redo generation·checkpoint age·장애 허용치로 policy와 capacity를 선택한 사용자 답변은 아직 확인하지 않았다.

다음 시작점은 4.2.12 어댑티브 해시 인덱스다.

## 추가 캡처 · 4.2.12 어댑티브 해시 인덱스

- 사용자가 보낸 제목 `어댑티브 해시 인덱스`와 사진 5장의 밑줄·별표·괄호·여백 질문을 [[4.2 InnoDB 스토리지 엔진 아키텍쳐#사용자 정리 원문]]에 `[사진 표시 전사]`로 보존했다.
- B+Tree 탐색과 AHI shortcut, 유리·불리한 workload, 비용과 latch contention, 관측·A/B 절차를 하나의 본문으로 연결했다.
- 사진은 Git에서 제외되는 local asset으로 배치했고, 다음 절 4.2.13 내용은 섞지 않았다.
- MySQL 8.0·8.4 공식 문서로 동적 ON/OFF, 정적 partition 수, major version별 기본값 차이를 교차 확인했다.

## 핵심 연결 · B+Tree 탐색을 줄이는 memory shortcut

```text
반복 B+Tree 검색 pattern 관찰
→ InnoDB가 (index ID + key prefix) → hot buffer-pool page·record 연결 생성
→ hit 시 root·branch 반복 탐색 감소
→ miss·비활성화 시 원래 B+Tree 탐색
```

- AHI는 사용자가 만드는 schema index가 아니라 InnoDB 내부 최적화다. optimizer의 별도 access path나 JPA query hint가 아니다.
- buffer pool에 상주한 page로 가는 CPU shortcut이므로 disk I/O를 없애지 않는다.
- memory-resident hot equality lookup에는 유리할 수 있지만 disk-bound·range·wildcard·wide scan에서는 이득이 제한된다.
- 많은 thread가 같은 AHI partition latch를 다투면 CPU·latency가 악화될 수 있으므로 `hash searches/s` 비율 하나만으로 판단하지 않는다.

## Spring/JPA 연결

- Spring Data JPA·Hibernate에는 AHI를 선택하는 annotation이나 query hint가 없다.
- JPA가 생성하는 equality query, hot-key 집중도, connection 동시성 같은 workload shape가 AHI 유효성에 간접 영향을 준다.
- instance 전역 설정이므로 한 repository query가 아니라 대표 workload 전체의 throughput·p95/p99·CPU·wait를 기준으로 ON/OFF를 비교한다.

## 실습 서버 확인 · AHI

현재 `realmysql 8.0.44` Community Server를 읽기 전용으로 확인했다.

```text
innodb_adaptive_hash_index       = ON
innodb_adaptive_hash_index_parts = 8
buffer pool data / total pages   = 7106 / 8192
P_S memory 계측값                 = 1688 bytes
AHI node heap buffers             = 2,1,1,1,1,1,1,46
hash searches/s                   = 0.10
non-hash searches/s               = 1.60
```

- 20초의 idle에 가까운 snapshot이라 AHI 효용을 판단할 근거가 아니다.
- `0.10 ÷ (0.10 + 1.60) ≈ 5.9%`도 단순 경유 비율이며 latency·CPU 개선을 뜻하지 않는다.
- Performance Schema의 `1688 bytes`는 해당 instrument의 계측값이지 AHI의 모든 실제 memory 비용을 완전하게 나타낸다고 단정하지 않는다.

## 시스템 변수·version 차이

- `innodb_adaptive_hash_index`는 GLOBAL dynamic이다. OFF 시 AHI를 비우고 B+Tree로 fallback하며, ON 시 workload에 따라 다시 생성한다.
- `innodb_adaptive_hash_index_parts`는 GLOBAL static이며 MySQL 8.0 기본 8, 범위 1~512다. partition 수는 측정된 latch contention 근거 없이 늘리지 않는다.
- MySQL 8.0은 AHI 기본값이 ON이지만 MySQL 8.4부터 기본 OFF다. 책의 8.0 기본값을 다른 major version에 그대로 적용하지 않는다.

## 종합 영역 갱신 · 4.2.12

한 문장·큰 지도·암기 카드·접힌 확인 질문·이어지는 내용·공식 자료를 4.2.12까지 갱신했다. level은 2를 유지한다. AHI의 B+Tree 우회·buffer-pool 전제·contention 비용은 연결했지만 실제 대표 workload로 ON/OFF benchmark를 수행하고 설정을 선택한 사용자 답변은 아직 확인하지 않았다.

다음 시작점은 4.2.13 InnoDB와 MyISAM, MEMORY 스토리지 엔진 비교다.

## 추가 캡처 · 4.4 MySQL 로그 파일

- 사용자 원문 `언제 어떤 로그를 어떻게 봐야하는지 등을 보기좋게만 정리하면 될거같아. 이거까지하고 push`를 [[4.4 MySQL 로그 파일#사용자 정리 원문]]에 그대로 보존했다.
- 사진 8장(`IMG_7622.jpg`~`IMG_7629.jpg`)의 밑줄·별표 내용을 확인하고, “증상 → 먼저 볼 로그 → 확인 방법 → 다음 행동” 구조로 재구성했다.
- error·general·slow log의 기록 목적과 시점을 구분하고, 아직 실행 중인 query는 PROCESSLIST·Performance Schema·sys에서 확인하도록 분리했다.
- MySQL 8.0 공식 문서와 Percona Toolkit 문서로 로그 destination·slow 기록 시점·fingerprint 집계 방식을 교차 확인했다.

## 핵심 연결 · 기록 시점으로 로그 고르기

~~~text
서버 시작·종료·복구·connection 이상 → Error log
서버가 실제로 받은 요청 전체          → General log
완료된 느린 statement                 → Slow query log
아직 실행 중·lock wait                → PROCESSLIST / Performance Schema / sys
~~~

- General log는 statement 수신 시점에 기록하므로 실패 SQL도 추적할 수 있다.
- Slow log는 statement가 완료된 뒤 기록하므로 진행 중 query는 보이지 않는다.
- slow log의 `Lock_time`만으로 InnoDB record lock 대기를 단정하지 않고 live wait 정보를 함께 본다.
- `pt-query-digest`로 fingerprint를 묶고 누적 시간(sum)·최악 지연(max)·빈도(cnt)를 각각 본다.

## 실습 서버 확인 · MySQL log

~~~text
realmysql version                  = 8.0.44
log_error                         = stderr
log_error_verbosity               = 2
log_timestamps                    = UTC
general_log                       = OFF
slow_query_log                    = OFF
long_query_time                   = 10
log_output                        = FILE
max_connect_errors                = 100
innodb_print_all_deadlocks        = OFF
~~~

- 이 container의 error log는 `docker logs realmysql`로 본다.
- general·slow log는 현재 OFF이며 `pt-query-digest`는 local에 설치되어 있지 않다.
- 설정은 바꾸지 않고 읽기 전용으로 확인했다.

## 종합 영역 갱신 · 4.4

증상별 선택표·Mermaid 흐름·시스템 변수·암기 카드·접힌 확인 질문·공식 자료를 [[4.4 MySQL 로그 파일]]에 정리했다. level은 2를 유지한다. 로그의 목적·기록 시점·분석 순서는 연결했지만 실제 장애 사례의 application·MySQL·OS 로그를 시간축으로 결합해 원인을 판별한 사용자 답변은 아직 확인하지 않았다.

다음 시작점은 05 트랜잭션과 잠금이다.
